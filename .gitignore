node_modules

# Quartz build output
quartz-engine/public
quartz-engine/quartz/.quartz-cache

# Site build output
public/

# Quartz symlink (redundant with quartz-engine)
quartz

# But keep the quartz source inside quartz-engine
!quartz-engine/quartz/
# Except the build cache
quartz-engine/quartz/.quartz-cache

# Static assets generated by build
moderation-webhook/
# Root-level build deps (not quartz-engine's)
/root/package.json
/root/package-lock.json

# OS files
.DS_Store
*.log
