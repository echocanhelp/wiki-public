# Runtime backup credentials (redacted)

Gateway/orchestrator `.env` backups were **purged from git history** on 2026-07-06 after public exposure.
Non-secret artifacts (e.g. `config.yaml`) may remain under sibling backup folders.

Rotate Discord, LINE, Telegram, and GitHub tokens if they ever appeared in a public commit.