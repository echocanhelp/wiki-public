Firecrawl CLI v1.16.2 installed and authenticated (API key stored). 13 skills installed (9 core + 4 build). Commands: firecrawl search (discover), scrape (markdown from URL), interact (browser actions), crawl (bulk), map (URL discovery), ask (diagnose failures). Credits ~1000/cycle. Base: https://api.firecrawl.dev/v2. Preferred web data tool for echohsu profile over generic search.
