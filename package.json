{
  "name": "wiki-public",
  "version": "1.0.0",
  "description": "Personal wiki built with Quartz",
  "scripts": {
    "build": "quartz build",
    "dev": "quartz build --serve",
    "sync": "quartz sync"
  },
  "devDependencies": {
    "@jackyzha0/quartz": "file:./quartz-engine"
  }
}